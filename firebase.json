print(default_api.run_terminal_command(command='git init'))
print(default_api.run_terminal_command(command='git config --global user.name "Your Name"'))
print(default_api.run_terminal_command(command='git config --global user.email "you@example.com"'))
print(default_api.run_terminal_command(command='git add .'))
print(default_api.run_terminal_command(command='git commit -m "chore: initial commit - pre EverRide"'))
{
  "firestore": {
    "database": "(default)",
    "location": "us-west1",
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": [
    {
      "source": "server/functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run lint"
      ]
    }
  ],
  "emulators": {
    "singleProjectMode": true
  },
  "hosting": {
    "public": "public"
  }
}
